{
  "name": "jest-puppeteer-poc",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "jest -c",
    "test.watch": "cross-env WATCH_MODE=true jest --watch"
  },
  "devDependencies": {
    "cross-env": "^6.0.3",
    "jest": "^24.9.0",
    "jest-puppeteer": "^4.2.0",
    "puppeteer": "^2.0.0",
    "puppeteer-firefox": "^0.5.0"
  }
}
